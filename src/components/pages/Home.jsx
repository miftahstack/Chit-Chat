import React from "react";
import { Button } from "../ui/button";

const Home = () => {
  return (
    <>
      <Button  className="">
        Login with Email
      </Button>
      <Button variant="outline" className="">
        SignUp with Google
      </Button>
    </>
  );
};

export default Home;
